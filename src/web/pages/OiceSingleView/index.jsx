import OiceSingleView from './OiceSingleView';
import * as OiceSingleViewActions from './OiceSingleView.actions';
import OiceSingleViewReducer from './OiceSingleView.reducer';

export const actions = OiceSingleViewActions;
export const reducer = OiceSingleViewReducer;

export default OiceSingleView;
