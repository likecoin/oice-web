import BackgroundModal from './BackgroundModal';
import * as BackgroundModalActions from './BackgroundModal.actions';
import BackgroundModalReducer from './BackgroundModal.reducer';

export const actions = BackgroundModalActions;
export const reducer = BackgroundModalReducer;

export default BackgroundModal;
