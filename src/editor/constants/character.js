
export const POSITIONS = {
  l: 0,
  m: 1,
  r: 2,
};

export const POSITIONS_LIST = Object.keys(POSITIONS);

export const COORDINATE = {
  x: 0,
  y: 1,
};

export const COORDINATE_LIST = Object.keys(COORDINATE);
